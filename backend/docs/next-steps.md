# 接下來要做的事（Debby）

更新：2026-09-09

後端程式碼、測試、正式庫建置都完成了。接下來**沒有寫程式的工作**，都是「問廠商拿
權限 / 資訊」和「把文件轉給其他組」。下面照優先順序排，附可直接複製的訊息。

---

## 🔴 第一優先：寄一封信給廠商

把下面這段直接貼給廠商（一次問完）：

> 嗨，中介平台後端的兩個新功能（單／雙人版模式、場域老師選單登入）已經開發完成，
> 用你們給的新資料庫（port 31497）測試都通過了，`AttentionLessonPlan` 的表格和
> 場域/老師資料我也用 root 建好了。接下來需要麻煩你們：
>
> 1. **Zeabur 專案存取權**：要更新環境變數（資料庫位址從舊的 31913 改成 31497）
>    並部署新版程式。可以加我進 Zeabur 專案，或請你們協助更新+重新部署。
> 2. **舊資料庫（port 31913）**：裡面的資料是不是都是可拋棄的測試資料？我們打算
>    直接用新資料庫，不做資料搬移，想先跟你們確認。
> 3. **程式碼 repo**：`github.com/mdtsai456/AttentionLessonPlanWebGames` 麻煩加我
>    寫入權限，另外想確認後端程式碼要放在這個 repo 的哪個資料夾。
> 4. **顯示用資訊**（不急，之後補即可）：
>    - 清華大學的 7 個場次實際怎麼區分？（地點？梯次？—— 用來顯示場域名稱）
>    - 16 位老師的實際姓名（每個場域 2 位）
> 5. **上線前**：root 密碼建議屆時換一組新的。

拿到回覆後告訴我，我會：
- 有 Zeabur 權限 → 給你一份「照抄就好」的環境變數清單
- 有 repo 權限 → 幫你把程式碼推上去
- 有老師姓名/場次資訊 → 更新資料庫

---

## 🟡 第二優先：把文件轉給其他組（現在就可以）

### 給前端組員

轉檔案：**`docs/frontend-integration-guide.md`**

附一句話：

> 這是中介平台的 API 串接文件，登入流程和所有端點的規格都在裡面。API 契約已經定案
> 不會變，你可以先開始做畫面。正式 API 網址這幾天會通，在那之前你可以照文件的資料
> 格式先用假資料開發。有問題再問我。

### 給負責 DAT / DCCS / EFT 遊戲的六位

轉檔案：**`docs/unity-integration-guide.md`**

附一句話：

> 這是把遊戲成績送回中介平台的串接文件。
> - **單人版**負責人：現有送資料的方式不用改。
> - **雙人版**負責人：`data` 裡要多送 `mode: "double"` 和 `pairId`（開局產一個 GUID、
>   同一局兩位學生共用），細節和範例都在文件 §3。
> - 六位都請看 §6 的場域代碼表（`KMU` / `NTHU-01`…），送資料時 `data.school` 要用
>   這些代碼。

> 想更保險的話，這份可以等正式 API 部署完再給，讓他們有個「線上就能測」的起點。

---

## 🟢 你自己這邊（我會協助）

| 事情 | 現在 | 之後 |
|---|---|---|
| 裝 DBeaver、連上資料庫 | 進行中 | 用來 demo 給廠商看「資料真的在資料庫裡」 |
| Zeabur 環境變數 | — | 拿到權限後，我給清單你照填（或我遠端指導） |
| 推程式碼到 repo | — | 拿到寫入權後我來推 |
| demo 給廠商看 | 可以了 | `uv run uvicorn main:app --port 5001` → 開 `/demo` |

---

## ⚪ 等其他組（不需要你做事，追蹤進度即可）

- 前端組員：做登入頁 + 資料檢視頁
- Unity 六人：雙人版加欄位
- 三邊都好了 → 一起做整合測試（真的用 Unity 送資料 → 前端看得到）

---

## 一句話總結

**你現在唯一要做的動作：寄第一優先那封信給廠商，然後把兩份文件轉給前端和 Unity。**
其他都在等回覆，或是我這邊的事。
