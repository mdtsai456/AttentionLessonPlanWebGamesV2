# 進度整理：2026-09-08（後端 — 單雙人模式 + 選單登入）

## 一句話

功能 A（單／雙人模式）、功能 B（場域／老師選單登入）的**程式碼、測試、文件都完成
並用真資料庫驗證通過**。剩下的是「上正式庫、更新 Zeabur、跟 Unity 對欄位、推到廠商
repo」——這幾件都需要別人配合或你做決定，見 §6。

---

## 1. 今天完成了什麼

| 項目 | 內容 | 狀態 |
|---|---|---|
| **環境** | 改用 `uv` 管理套件、鎖定版本（`pyproject.toml` + `uv.lock`），修掉測試收集崩潰 | ✅ |
| **CORS** | 加上 `CORSMiddleware`，放行來源由 `CORS_ALLOW_ORIGINS` 環境變數控制 | ✅ |
| **功能 A：單／雙人模式** | `assessment_result` 加 `mode` / `pair_id`；`POST /api/sessions` 支援 `mode`/`pairId` + 驗證；`/report`、`/sessions` 每筆帶 `mode`，`summaryByGame`／`trends` 依 `(遊戲, 模式)` 分組；`seed.py` 多灌雙人版假資料 | ✅ |
| **功能 B：選單登入** | 新增 `school`、`teacher` 表；3 支唯讀端點（`/api/schools`、`/api/schools/{s}/teachers`、`/api/teachers/{id}/students`）；`seed_directory.py` 冪等灌參照資料 | ✅ |
| **資料庫** | 用廠商給的 root 連上線上 MariaDB，建立 `AttentionLessonPlan` 與 `AttentionLessonPlan_test` | ✅ |
| **測試** | 連真資料庫跑：main 148 ／ 功能A 180 ／ 功能B 169 ／ **A+B 合併 201**，全過 | ✅ |
| **Demo 畫面** | `/demo` 簡易檢視頁（非正式前端），把「選人→看進度」流程畫出來 | ✅ |
| **文件** | 前端串接指南、Unity 串接指南、本進度文件 | ✅ |

---

## 2. 分支結構

本機 git repo（尚未推到遠端）。分支：

```
main                  baseline + uv + CORS + 前端指南
├─ mode_single_debby  功能 A
├─ login_single_debby 功能 B
└─ integration        A + B 合併 + demo + seed 對齊  ← 目前的「完整版」，demo 從這裡跑
```

`integration` 是驗收 / demo / 之後上線要用的分支。

---

## 3. 資料庫現況

**新的 MariaDB（廠商 2026-09-08 給的，port 31497）：**

| 資料庫 | 內容 |
|---|---|
| `AttentionLessonPlan_test` | 全部表格 + 假資料（18 學生 / 2484 場次 / 8 場域 / 16 老師）。開發、測試、demo 用 |
| `AttentionLessonPlan` | ✅ **9 張表已建好** + 8 場域 + 16 老師（老師名字是佔位）。還沒有學生／成績資料 |

- 連線資訊在本機 `.env`（host `43.163.233.40` port **31497**，root）。**不進 git。**
- `.env` 目前 `DB_NAME` 指向 `_test`（開發階段方便）。

**舊的 MariaDB（port 31913）：** 還活著，裡面有舊的假資料。**目前上線的正式網站
連的是這個 + 跑的是舊版 code**（沒有 `/api/schools`、沒有 `mode`）。切換到新 code +
新 DB 是一次部署事件，見 §6-3。

---

## 4. 怎麼啟動 Demo 畫面

開一個 PowerShell 視窗，`cd` 到專案資料夾：

```powershell
git checkout integration          # 切到完整版分支
uv run uvicorn main:app --port 5001
```

看到 `Application startup complete` 後，瀏覽器開：

| 網址 | 是什麼 |
|---|---|
| `http://127.0.0.1:5001/demo` | Demo 畫面 |
| `http://127.0.0.1:5001/docs` | API 文件（可逐一點端點試） |

**Demo 操作**：老師端 → 選場域（高雄醫學大學）→ 選老師（吳老師）→ 點學生 → 看報告。
或直接開 `http://127.0.0.1:5001/demo?school=KMU&student=G1_S01` 跳到報告頁。

關掉 PowerShell 視窗 = 停掉伺服器。

### 假資料被清掉時重灌

```powershell
uv run python seed.py
uv run python seed_directory.py
```

---

## 5. 怎麼跑測試

```powershell
uv run pytest -q
```

連遠端資料庫，一輪約 2–3 分鐘。**切換分支後**（例如從 main 切到功能分支）第一次跑
可能要先重建 `_test`，因為 `schema.sql` 用 `CREATE TABLE IF NOT EXISTS`，不會自動補
新欄位：

```powershell
uv run python -c "import pymysql; c=pymysql.connect(host='43.163.233.40',port=31497,user='root',password='<看 .env>'); cur=c.cursor(); cur.execute('DROP DATABASE IF EXISTS AttentionLessonPlan_test'); cur.execute('CREATE DATABASE AttentionLessonPlan_test CHARACTER SET utf8mb4'); c.commit()"
```

---

## 6. 還沒做的事

### 6-1. 推到廠商 repo `github.com/mdtsai456/AttentionLessonPlanWebGames`

那個 repo 目前只有一份 README 骨架，分區是：DAT / DCCS / EFT（各 Single / Double）、
Login Page、Data Management System Page。**是整包 monorepo**：6 款遊戲 + 登入頁 +
資料管理頁（＝前端）。後端沒被明列，最接近的是「Data Management System」。

⚠️ **這個 repo 是公開的。** 確認：
- `.env`（含 root 密碼）**絕不能進 git** —— 已在 `.gitignore`，OK
- 其他文件裡的密碼都用佔位字串，OK
- code 本身無敏感資訊

要做的：
- 跟廠商問「後端 code 放哪個資料夾」（例如 `data-management-system/backend/`）
- 需要那個 repo 的**寫入權限**（廠商加 collaborator）
- 之後我把本地 4 條分支推上去（或依廠商要的結構調整）

### 6-2. 正式庫上線（`AttentionLessonPlan`）— 大部分已完成

| 步驟 | 狀態 |
|---|---|
| 在 `AttentionLessonPlan`（新 DB, 31497）建 9 張表 | ✅ 已用 root 建好 |
| 灌 8 場域 + 16 老師 | ✅ 已灌（`seed_directory.py --prod`） |
| 場域字串定案 | ✅ 廠商交給後端決定 → 定為 `KMU` / `NTHU-01`…`NTHU-07`（見 `docs/school-directory.md`） |
| 帳號權限 | ⚠️ 本表原寫「決定：正式網站也用 root」——**2026-09-22 部署前已修正**：正式網站改回 `app_ro`（讀）／`game_writer`（寫），符合 [ADR-0001](adr/0001-least-privilege-db-accounts.md) 的最小權限原則；`root` 只留人工維運，不進 Zeabur。建帳號的 GRANT 語法見 [`db_accounts.sql`](../db_accounts.sql)（已補上 ADR-0001 寫成時還沒有的 `login_session` 授權，否則帳密登入會 500）。 |
| 顯示名稱、16 位老師實際姓名 | ⏳ 佔位中，等廠商微調（不是 join key，改動風險低） |

`AttentionLessonPlan` 現在**已準備好接收真實資料**。只差把新版 code 部署上去（§6-3）。

### 6-3. 部署新版到 Zeabur（唯一還在等的大項）

**現況**（實測）：`https://attention-lesson-plan-transfer-data.zeabur.app` 活著，
`/api/students` 回得出資料 —— 但**是舊版 code + 舊 DB（31913）**。`/api/schools` 回 404
（舊版沒有這個端點）。

要讓正式網站變成新版（含單雙人 + 登入），需要**一次部署**，包含兩件事：

1. **更新 Zeabur 環境變數**（Zeabur 那組設定跟本機 `.env` 是分開的兩份）：

   | 變數 | 值 |
   |---|---|
   | `DB_HOST` | `43.163.233.40` |
   | `DB_PORT` | `31497`（從舊的 `31913` 改） |
   | `DB_USER` / `DB_PASSWORD` | `app_ro` / （見 `db_accounts.sql` 建立時設的密碼） |
   | `DB_WRITE_USER` / `DB_WRITE_PASSWORD` | `game_writer` / （見 `db_accounts.sql` 建立時設的密碼） |
   | `DB_NAME` | `AttentionLessonPlan` |
   | `CORS_ALLOW_ORIGINS` | 前端正式網址（前端部署後給）；暫時可留空或填 `*` |

   > ⚠️ **不要**把 `root` 填進 Zeabur 的 `DB_USER`／`DB_WRITE_USER`。`root`
   > 只用來手動執行 `db_accounts.sql` 建帳號、或人工維運，不進任何部署環境的
   > 環境變數（見 [ADR-0001](adr/0001-least-privilege-db-accounts.md)）。

2. **部署新 code**：把 `integration` 分支（或合併進主線的版本）接上 Zeabur。

**你現在要做的**：確認你有沒有 Zeabur 專案的登入權限。
- **沒有** → 跟廠商要（見下方可貼訊息），或請廠商代做這次部署
- **有** → 等前端正式網址出來，我給你一份「照抄就好」的 Zeabur 設定清單

<details><summary>可貼給廠商的訊息</summary>

> 中介平台的後端新版（單雙人模式 + 場域老師選單登入）已完成並用新的資料庫
> （port 31497）測試通過。要讓 Zeabur 上的正式站更新，需要：
> 1. Zeabur 專案的存取權（或請你們協助更新環境變數 + 重新部署）
> 2. 確認：舊資料庫（port 31913）裡的資料是否都是可拋棄的測試資料？我們打算
>    直接用新資料庫，不做搬移。
> 3. 新資料庫的 `AttentionLessonPlan` 我已用你們給的 root 建好表格與場域老師資料。

</details>

> **舊 DB（31913）的資料**：實測是「民富國小」等假資料、每位學生 120 場，看起來
> 是 `seed.py` 的產物。要跟廠商確認沒有真實受試者資料後，就直接用新 DB、不搬移。

### 6-4. 跟 Unity 組員對欄位

交接文件：**`docs/unity-integration-guide.md`**，直接轉給那六位（3 款 × 單/雙人各一位）。

- 單人版負責人（3）：**不用改**。
- 雙人版負責人（3）：`data` 加 `mode: "double"` + `pairId`（一場雙人局一個 GUID、
  兩位學生共用）。
- 六人都要用定案的場域代碼（`docs/school-directory.md`）。

### 要不要等「都 ready、推上 git」再給前端 / Unity？

**不用等 git。** 前端和 Unity 都是打 HTTP API，不看後端 repo。他們的實際依賴是：

| 團隊 | 真正的依賴 | 現況 |
|---|---|---|
| 前端 | 一個可連的 API + 契約 | 契約已定、已測。API：可先本機跑，或等 §6-3 部署（本週） |
| Unity | POST 契約 + 場域代碼 | 都已定案 ✅ |

**建議**：
- **前端指南**：現在就給。他們最久沒事做，契約穩定，可先照契約 mock 開發。
- **Unity 指南**：也可以現在給（契約與場域碼都定了）。若想更保險，等 §6-3 部署完、
  給他們一個「正式 API 已可用」的乾淨起點再給，差幾天。

---

## 7. 今天新增／改動的重要檔案

| 檔案 | |
|---|---|
| `pyproject.toml` / `uv.lock` | 新：uv 套件管理 |
| `main.py` | 改：CORS middleware、`/demo` 路由 |
| `demo/index.html` | 新：demo 檢視頁 |
| `models.py` `queries.py` `writes.py` `converters.py` | 改：mode/pair_id + 場域老師查詢 |
| `routers/sessions.py` `routers/students.py` | 改：mode 驗證與分組 |
| `routers/directory.py` | 新：場域/老師端點 |
| `seed.py` `seed_directory.py` | 改/新：雙人版假資料、參照資料灌注 |
| `tests/` | 改/新：201 個測試 |
| `docs/frontend-integration-guide.md` | 新：給前端組員 |
| `docs/unity-integration-guide.md` | 新：給 Unity 六位 |
| `docs/school-directory.md` | 新：場域字串唯一真實來源（三方共用） |
| `docs/adr/0002-use-uv-for-env.md` | 新：uv 決策紀錄 |

---

## 8. 你這邊的待辦（更新版）

| # | 事情 | 狀態 |
|---|---|---|
| 1 | 正式庫建表 | ✅ 我做完了 |
| 2 | 場域字串定案 | ✅ 定為 `KMU` / `NTHU-01`…`NTHU-07`（廠商交給後端決定） |
| 3 | 帳號權限 | ⚠️ 原「決定用 root」已於 2026-09-22 部署前修正為 `app_ro`／`game_writer`，見上方 §6-2、§6-3 與 [`db_accounts.sql`](../db_accounts.sql) |
| 4 | **問廠商**：Zeabur 存取權（或請他們代做部署） | ⏳ 你 |
| 5 | **問廠商**：舊 DB（31913）有無真實資料？7 個清大場次怎麼區分？16 位老師實際姓名？ | ⏳ 你 |
| 6 | **問廠商**：`AttentionLessonPlanWebGames` repo 寫入權 + 後端 code 放哪 | ⏳ 你 |
| 7 | 轉 `docs/frontend-integration-guide.md` 給前端組員 | ⏳ 你（現在就可以） |
| 8 | 轉 `docs/unity-integration-guide.md` 給 Unity 六位 | ⏳ 你（現在可，或等部署完） |
